import { Typography } from "@mui/material";
import HeaderContainer from "../styled/Header.styled";

const Header = () => {
    return (
        <HeaderContainer>
            <Typography>Zoop</Typography>
            <Typography>Georgian Apartmants</Typography>
            <Typography>ProfileIcon</Typography>
        </HeaderContainer>
    );
};

export default Header;
